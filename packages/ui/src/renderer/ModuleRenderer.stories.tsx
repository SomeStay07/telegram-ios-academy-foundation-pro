import type { Meta, StoryObj } from '@storybook/react'; import { ModuleRenderer } from './ModuleRenderer'; const meta:Meta<typeof ModuleRenderer>={title:'Lesson/ModuleRenderer',component:ModuleRenderer}; export default meta; type S=StoryObj<typeof ModuleRenderer>; export const Quiz:S={ args:{ module:{ kind:'quiz', id:'q1', question:'Что верно о let?', options:['Можно менять','Нельзя менять'], correctAnswer:1, explanation:'let immutable'} as any } };