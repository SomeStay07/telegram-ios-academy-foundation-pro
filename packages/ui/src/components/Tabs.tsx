import React,{useState} from 'react'
export const Tabs:React.FC<{tabs:{id:string;label:string;content:React.ReactNode}[], defaultId?:string}>=({tabs,defaultId})=>{const[active,setActive]=useState(defaultId??tabs[0]?.id);return(<div><div role='tablist' style={{display:'flex',gap:8,marginBottom:12}}>{tabs.map(t=>(<button key={t.id} role='tab' aria-selected={active===t.id} onClick={()=>setActive(t.id)} style={{padding:'8px 12px',borderRadius:10,border:active===t.id?'1px solid var(--color-primary)':'1px solid var(--color-muted)',background:active===t.id?'rgba(13,110,253,.08)':'transparent'}}>{t.label}</button>))}</div><div>{tabs.find(t=>t.id===active)?.content}</div></div>)}