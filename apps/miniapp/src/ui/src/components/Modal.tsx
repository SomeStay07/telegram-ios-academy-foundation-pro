import React from 'react'
export const Modal:React.FC<React.PropsWithChildren<{open:boolean;onClose:()=>void;title?:string}>>=({open,onClose,title,children})=>!open?null:(<div role='dialog' aria-modal='true' aria-label={title} style={{position:'fixed',inset:0,background:'rgba(0,0,0,.4)',display:'grid',placeItems:'center',padding:16}} onClick={onClose}><section style={{background:'var(--color-bg)',color:'var(--color-fg)',borderRadius:12,boxShadow:'var(--shadow)',width:'min(560px,92vw)',padding:24}} onClick={e=>e.stopPropagation()}>{title&&<h3 style={{marginTop:0}}>{title}</h3>}<div>{children}</div></section></div>)