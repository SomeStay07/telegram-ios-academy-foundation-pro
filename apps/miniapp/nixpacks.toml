[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = [
  'npm install -g pnpm@9.0.0',
  'cd apps/miniapp && pnpm install --frozen-lockfile'
]

[phases.build]
cmds = [
  'cd apps/miniapp && pnpm build'
]

[start]
cmd = 'cd apps/miniapp && pnpm start'