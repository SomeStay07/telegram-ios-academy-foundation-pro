import { Module } from '@nestjs/common';
// TODO: Import assessment controllers and services when they are refactored

@Module({
  controllers: [
    // TODO: Add InterviewController after refactoring
  ],
  providers: [
    // TODO: Add assessment domain services, repositories, handlers
  ],
  exports: [],
})
export class AssessmentModule {}