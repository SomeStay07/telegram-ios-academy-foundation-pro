{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd ../.. && pnpm install && pnpm run build --filter=@telegram-ios-academy/bot"
  },
  "deploy": {
    "startCommand": "node dist/index.js",
    "restartPolicyType": "always"
  }
}