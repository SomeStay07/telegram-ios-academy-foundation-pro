[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = [
  'npm install -g pnpm@9.0.0',
  'cd apps/bot && pnpm install --frozen-lockfile'
]

[phases.build]
cmds = [
  'cd apps/bot && pnpm build'
]

[start]
cmd = 'cd apps/bot && pnpm start'